version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - method: pip
commands:
  pre_build:
    - pip install poetry
    - poetry install -E docs --no-root
  build:
    - poetry run sphinx-build -b html docs/ _build/html
